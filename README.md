# Simple Calculator

This is a simple Python command-line calculator. It allows you to perform basic arithmetic operations such as addition, subtraction, multiplication, and division.

## Usage

Run the calculator using Python 3:

```bash
python3 calculator.py
```

Follow the prompts to select an operation and enter numbers. Select `5` to exit.
